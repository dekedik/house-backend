{
  "name": "novostroyki-monorepo",
  "version": "1.0.0",
  "description": "Монорепозиторий агентства новостроек",
  "private": true,
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "dev": "npm run dev:client & npm run dev:admin",
    "build:client": "cd client && npm run build",
    "build:admin": "cd admin && npm run build",
    "build": "npm run build:client && npm run build:admin",
    "install:all": "cd client && npm install && cd ../admin && npm install"
  }
}
