# Защита SSL сертификатов при обновлениях

## ✅ SSL сертификат НЕ будет слетать при обновлениях

### Что было сделано для защиты:

1. **Автообновление SSL через cron**
   - Сертификат обновляется автоматически каждый день в 3:00
   - После обновления автоматически копируется в `/opt/house-backend/ssl/`
   - Nginx автоматически перезагружается с новым сертификатом

2. **Защита в скрипте обновления (`deploy-update.sh`)**
   - Скрипт **НЕ удаляет** директорию `ssl/`
   - Скрипт **НЕ изменяет** файл `.env.prod`
   - Перед обновлением из git создаются резервные копии
   - После обновления важные файлы восстанавливаются

3. **Исключение из git**
   - Директория `ssl/` добавлена в `.gitignore`
   - SSL сертификаты не попадут в git репозиторий
   - При копировании файлов на сервер SSL не перезаписывается

4. **Хранение сертификатов**
   - Оригиналы хранятся в `/etc/letsencrypt/live/admin-doman-horizont.ru/`
   - Копии для nginx в `/opt/house-backend/ssl/`
   - Оба места защищены от случайного удаления

## Как работает автообновление

### Cron задача:
```bash
0 3 * * * certbot renew --quiet --deploy-hook 'cd /opt/house-backend && cp /etc/letsencrypt/live/admin-doman-horizont.ru/fullchain.pem ssl/ && cp /etc/letsencrypt/live/admin-doman-horizont.ru/privkey.pem ssl/ && docker-compose --env-file .env.prod -f docker-compose.prod.yml exec nginx nginx -s reload'
```

**Что происходит:**
- Каждый день в 3:00 проверяется срок действия сертификата
- Если до истечения осталось меньше 30 дней - сертификат обновляется
- После обновления копируется в `ssl/` директорию
- Nginx перезагружается с новым сертификатом

### Проверка автообновления:

```bash
# Посмотреть cron задачи
crontab -l | grep certbot

# Проверить статус сертификата
certbot certificates

# Ручное обновление (для теста)
certbot renew --dry-run
```

## Что НЕ удаляется при обновлениях

✅ **Защищено от удаления:**
- `/opt/house-backend/ssl/` - SSL сертификаты
- `/opt/house-backend/.env.prod` - переменные окружения
- `/etc/letsencrypt/` - оригинальные сертификаты Let's Encrypt
- Cron задачи для автообновления SSL

❌ **Может обновиться:**
- Код приложения
- Docker образы
- Конфигурация nginx (но SSL пути остаются)
- Миграции БД

## Процесс обновления

При выполнении `./scripts/deploy-update.sh`:

1. ✅ Проверяется наличие `.env.prod` (не удаляется)
2. ✅ Создаются резервные копии `ssl/` и `.env.prod`
3. ✅ Обновляется код из git
4. ✅ Восстанавливаются `ssl/` и `.env.prod` из резервных копий
5. ✅ Собираются новые Docker образы
6. ✅ Применяются миграции БД
7. ✅ Перезапускается backend
8. ✅ Проверяется наличие SSL сертификатов

## Ручное обновление SSL (если нужно)

Если по какой-то причине нужно обновить SSL вручную:

```bash
cd /opt/house-backend

# Обновить сертификат
certbot renew

# Скопировать в директорию проекта
cp /etc/letsencrypt/live/admin-doman-horizont.ru/fullchain.pem ssl/
cp /etc/letsencrypt/live/admin-doman-horizont.ru/privkey.pem ssl/

# Перезагрузить nginx
docker-compose --env-file .env.prod -f docker-compose.prod.yml exec nginx nginx -s reload
```

## Проверка статуса SSL

```bash
# Проверить срок действия сертификата
certbot certificates

# Проверить сертификат через openssl
openssl x509 -in /opt/house-backend/ssl/fullchain.pem -noout -dates

# Проверить через браузер
# Откройте https://admin-doman-horizont.ru и посмотрите на иконку замка
```

## Важно

- **SSL сертификаты обновляются автоматически** - не нужно ничего делать
- **При обновлениях кода SSL не удаляется** - скрипт защищен
- **Срок действия:** 90 дней, обновляется за 30 дней до истечения
- **Автообновление:** каждый день в 3:00 (проверка и обновление при необходимости)

## Итог

🔒 **SSL сертификат полностью защищен от случайного удаления при обновлениях!**

